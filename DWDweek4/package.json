{
  "name": "todo",
  "version": "1.0.0",
  "description": "- Paste the following into your terminal - `git clone git@github.com:nax3t/ajax-jquery-tutorial.git` - Now `cd` into `ajax-jquery-tutorial` and run `npm install` - Start your mongo daemon `mongod` in a separate terminal tab - Install `nodemon` globally (if you don't already have it installed) with `npm i -g nodemon` - Run your application in its own terminal tab with `nodemon` - Navigate to `localhost:3000` in your browser",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ian",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "ejs": "^2.5.7",
    "express": "^4.16.2",
    "express-sanitizer": "^1.0.3",
    "express-session": "^1.15.6",
    "method-override": "^2.3.10",
    "mongodb": "^3.0.2",
    "mongojs": "^2.5.0",
    "mongoose": "^4.13.11"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nax3t/ajax-jquery-tutorial.git"
  },
  "bugs": {
    "url": "https://github.com/nax3t/ajax-jquery-tutorial/issues"
  },
  "homepage": "https://github.com/nax3t/ajax-jquery-tutorial#readme"
}
